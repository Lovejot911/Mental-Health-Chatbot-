import "./globals.css";

export const metadata = {
  title: "Mental Health AI",
  description: "Mental Health Ability Testing Using NLP",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
